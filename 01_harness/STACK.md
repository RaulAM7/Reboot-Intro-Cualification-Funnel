# STACK

Default: N/A (docs-first workspace).

If code is introduced later, track here:
- Runtime/framework: Next.js App Router + React + TypeScript + Tailwind CSS
- Project layout conventions:
  `src/app/` para rutas y layout
  `src/components/` para UI y bloques del funnel
  `src/data/` para pasos declarativos del formulario
  `src/lib/` para scoring, resumen, validacion e integraciones servidor
  `supabase/migrations/` para SQL
  `public/` para assets de marca
- Build/test/dev commands:
  `npm install`
  `npm run dev`
  `npm run lint`
  `npm run build`
